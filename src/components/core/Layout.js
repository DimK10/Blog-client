import React from 'react';
import Menu from './Menu';
import Header from './Header';

const Layout = () => {

    

    return(
        <div>
            <Menu />
            <Header />
        </div>
    );
};

export default Layout;